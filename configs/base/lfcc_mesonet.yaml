data:
  name: in_the_wild
model:
  class: mesonet
  id: lfcc_mesonet
  parameters:
    fc1_dim: 1024
    frontend_algorithm:
    - lfcc
    input_channels: 1
training:
  batch_size: 16
  init_weights: ''
  max_epochs: 5
  optimzer:
    lr: 0.0001
    weight_decay: 0.0001
