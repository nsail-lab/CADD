data:
  name: in_the_wild
model:
  class: lcnn
  id: mfcc_lcnn
  parameters:
    frontend_algorithm:
    - mfcc
    input_channels: 1
training:
  batch_size: 16
  init_weights: ''
  max_epochs: 5
  optimzer:
    lr: 0.0001
    weight_decay: 0.0001
