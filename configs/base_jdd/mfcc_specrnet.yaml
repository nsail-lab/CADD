data:
  name: jdd
model:
  class: specrnet
  id: mfcc_specrnet
  parameters:
    frontend_algorithm:
    - mfcc
    input_channels: 1
training:
  batch_size: 16
  init_weights: ''
  max_epochs: 30
  optimzer:
    lr: 0.0001
    weight_decay: 0.0001
