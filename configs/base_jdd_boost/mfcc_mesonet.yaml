data:
  name: jdd_boost
model:
  class: mesonet
  id: mfcc_mesonet
  parameters:
    fc1_dim: 1024
    frontend_algorithm:
    - mfcc
    input_channels: 1
training:
  batch_size: 16
  init_weights: ''
  max_epochs: 30
  optimzer:
    lr: 0.0001
    weight_decay: 0.0001
