data:
  name: in_the_wild_context
model:
  class: specrnet_context
  id: mfcc_specrnet_context
  parameters:
    context: ct
    frontend_algorithm:
    - mfcc
    input_channels: 1
    embedding_dim: 100
training:
  batch_size: 16
  init_weights: ''
  max_epochs: 20
  optimzer:
    lr: 0.0001
    weight_decay: 0.0001
