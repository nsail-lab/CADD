data:
  name: in_the_wild_context
model:
  class: whisper_lcnn_context
  id: whisper_lcnn_context
  parameters:
    context: ct
    freeze_encoder: true
    frontend_algorithm: []
    input_channels: 1
    embedding_dim: 100
training:
  batch_size: 16
  init_weights: ''
  max_epochs: 20
  optimzer:
    lr: 0.0001
    weight_decay: 0.0001
