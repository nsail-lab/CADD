data:
  name: jdd_boost_context
model:
  class: whisper_frontend_specrnet_context
  id: whisper_mfcc_specrnet_context
  parameters:
    context: ct
    freeze_encoder: true
    frontend_algorithm:
    - mfcc
    input_channels: 2
    embedding_dim: 100
training:
  batch_size: 16
  init_weights: ''
  max_epochs: 30
  optimzer:
    lr: 0.0001
    weight_decay: 0.0001
