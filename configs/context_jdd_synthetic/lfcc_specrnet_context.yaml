data:
  name: jdd_synthetic_context
model:
  class: specrnet_context
  id: lfcc_specrnet_context
  parameters:
    context: ct
    frontend_algorithm:
    - lfcc
    input_channels: 1
    embedding_dim: 100
training:
  batch_size: 16
  init_weights: ''
  max_epochs: 30
  optimzer:
    lr: 0.0001
    weight_decay: 0.0001
