data:
  name: jdd_synthetic_context
model:
  class: whisper_mesonet_context
  id: whisper_mesonet_context
  parameters:
    context: ct
    fc1_dim: 1024
    freeze_encoder: true
    frontend_algorithm: []
    input_channels: 1
    embedding_dim: 100
training:
  batch_size: 16
  init_weights: ''
  max_epochs: 30
  optimzer:
    lr: 0.0001
    weight_decay: 0.0001
